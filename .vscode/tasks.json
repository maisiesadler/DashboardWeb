{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/dashboard.web/dashboard.web.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}